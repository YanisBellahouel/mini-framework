// ============================================================
// STORE — Gestion de l'état global
// Responsabilité : stocker, lire et modifier l'état de l'app
// ============================================================

// TODO Phase 4 : implémenter createStore(), getState(), commit(), subscribe()

export function createStore({ state, mutations }) {
  // Crée un store global réactif
}
