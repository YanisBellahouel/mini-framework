// ============================================================
// ROUTER — Système de routing basé sur le hash
// Responsabilité : synchroniser l'URL avec l'état de l'app
// ============================================================

// TODO Phase 5 : implémenter createRouter(), navigate()

export function createRouter(routes) {
  // Initialise le routeur avec les routes définies
}

export function navigate(path) {
  // Change la route programmatiquement
}
